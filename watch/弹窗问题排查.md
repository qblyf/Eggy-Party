# 弹窗问题排查指南

## 🔍 问题描述

点击抽取后，看不到抽取的卡片弹窗。

## 🛠️ 排查步骤

### 1. 打开浏览器控制台

按 `F12` 或右键 → 检查，查看控制台输出。

### 2. 查看调试信息

点击抽取后，应该看到以下日志：
```
showResult called 快乐蛋仔 false
Modal elements found, setting content...
Showing modal...
Modal classes: result-modal show
```

### 3. 检查元素是否存在

在控制台输入：
```javascript
document.getElementById('resultModal')
document.getElementById('modalContent')
document.getElementById('resultImage')
```

应该都返回元素，而不是 `null`。

### 4. 检查弹窗样式

在控制台输入：
```javascript
const modal = document.getElementById('resultModal');
console.log('display:', window.getComputedStyle(modal).display);
console.log('z-index:', window.getComputedStyle(modal).zIndex);
console.log('classes:', modal.className);
```

应该显示：
- display: flex (当有 show 类时)
- z-index: 2000
- classes: result-modal show

## 🐛 可能的问题

### 问题1: 元素未加载

**症状**: 控制台显示 "Modal content not found"

**原因**: JavaScript 在 HTML 加载前执行

**解决**: 确保 `<script src="script.js"></script>` 在 `</body>` 之前

### 问题2: CSS 未加载

**症状**: 弹窗存在但不可见

**原因**: CSS 文件未正确加载

**解决**: 
1. 检查 `<link rel="stylesheet" href="style.css">` 是否正确
2. 清除浏览器缓存 (Ctrl+Shift+R)
3. 检查 style.css 文件是否存在

### 问题3: z-index 被覆盖

**症状**: 弹窗被其他元素遮挡

**原因**: 其他元素的 z-index 更高

**解决**: 在 style.css 中增加弹窗的 z-index
```css
.result-modal {
    z-index: 9999 !important;
}
```

### 问题4: display 未生效

**症状**: 弹窗有 show 类但仍不显示

**原因**: CSS 选择器优先级问题

**解决**: 使用 !important
```css
.result-modal.show {
    display: flex !important;
}
```

## ✅ 快速修复

### 方法1: 使用测试页面

打开 `test-modal.html` 测试弹窗是否工作：

```bash
# 在浏览器中打开
open watch/test-modal.html
```

如果测试页面的弹窗能正常显示，说明 CSS 和 HTML 结构是正确的。

### 方法2: 手动触发弹窗

在浏览器控制台输入：
```javascript
// 手动显示弹窗
const modal = document.getElementById('resultModal');
modal.classList.add('show');

// 设置内容
document.getElementById('resultImage').textContent = '😊';
document.getElementById('resultName').textContent = '快乐蛋仔';
document.getElementById('resultRarity').textContent = '普通';
document.getElementById('resultCount').textContent = '拥有 1 个';
document.getElementById('resultStars').textContent = '⭐';
```

如果这样能显示，说明 JavaScript 逻辑有问题。

### 方法3: 检查 Service Worker

Service Worker 可能缓存了旧版本：

```javascript
// 在控制台输入
navigator.serviceWorker.getRegistrations().then(registrations => {
    registrations.forEach(registration => registration.unregister());
});

// 然后刷新页面
location.reload();
```

### 方法4: 强制刷新

1. 按 `Ctrl+Shift+R` (Windows) 或 `Cmd+Shift+R` (Mac)
2. 或清除浏览器缓存
3. 重新打开页面

## 🔧 临时解决方案

如果弹窗仍然不显示，可以使用 alert 临时替代：

在 `script.js` 的 `showResult` 方法中添加：

```javascript
showResult(pony, isNew) {
    // 临时使用 alert
    alert(`获得: ${pony.name}\n稀有度: ${this.getRarityText(pony.rarity)}\n拥有: ${this.gameData.collection[pony.id].count} 个`);
    
    // 原有代码...
}
```

## 📋 完整检查清单

- [ ] 打开浏览器控制台
- [ ] 点击抽取按钮
- [ ] 查看控制台是否有错误
- [ ] 检查是否有 "showResult called" 日志
- [ ] 检查是否有 "Showing modal..." 日志
- [ ] 检查 resultModal 元素是否存在
- [ ] 检查 resultModal 是否有 show 类
- [ ] 检查 display 样式是否为 flex
- [ ] 检查 z-index 是否为 2000
- [ ] 尝试手动触发弹窗
- [ ] 清除缓存并刷新
- [ ] 尝试测试页面 test-modal.html

## 💡 调试技巧

### 1. 添加边框

临时添加明显的边框，确认元素位置：

```css
.result-modal {
    border: 5px solid red !important;
}

.modal-card {
    border: 5px solid blue !important;
}
```

### 2. 修改背景色

让弹窗更明显：

```css
.modal-backdrop {
    background: rgba(255, 0, 0, 0.9) !important;
}
```

### 3. 移除动画

动画可能导致问题，临时移除：

```css
.modal-content {
    animation: none !important;
}
```

## 📞 仍然无法解决？

如果以上方法都不行，请提供以下信息：

1. 浏览器类型和版本
2. 控制台的完整错误信息
3. 控制台输入以下命令的结果：
   ```javascript
   console.log('Modal:', document.getElementById('resultModal'));
   console.log('Classes:', document.getElementById('resultModal')?.className);
   console.log('Display:', window.getComputedStyle(document.getElementById('resultModal')).display);
   ```

## 🎯 最可能的原因

根据经验，最常见的原因是：

1. **浏览器缓存** (70%) - 清除缓存解决
2. **CSS 未加载** (20%) - 检查文件路径
3. **JavaScript 错误** (10%) - 查看控制台

**建议**: 先尝试强制刷新 (Ctrl+Shift+R)！
